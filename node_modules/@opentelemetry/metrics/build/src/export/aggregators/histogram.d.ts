import { Aggregator, Point } from '../types';
/**
 * Basic aggregator which observes events and counts them in pre-defined buckets
 * and provides the total sum and count of all observations.
 */
export declare class HistogramAggregator implements Aggregator {
    private _lastCheckpoint;
    private _currentCheckpoint;
    private _lastCheckpointTime;
    private readonly _boundaries;
    constructor(boundaries: number[]);
    update(value: number): void;
    reset(): void;
    toPoint(): Point;
    private _newEmptyCheckpoint;
}
//# sourceMappingURL=histogram.d.ts.map