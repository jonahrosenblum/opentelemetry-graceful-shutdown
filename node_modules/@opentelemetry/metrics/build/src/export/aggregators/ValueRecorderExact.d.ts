import { Aggregator, Point } from '../types';
/** Basic aggregator keeping all raw values (events, sum, max and min). */
export declare class ValueRecorderExactAggregator implements Aggregator {
    private _distribution;
    private _lastUpdateTime;
    constructor();
    update(value: number): void;
    toPoint(): Point;
}
//# sourceMappingURL=ValueRecorderExact.d.ts.map